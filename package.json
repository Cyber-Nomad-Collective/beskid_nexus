{
  "name": "beskid-nexus",
  "private": true,
  "description": "Beskid compiler knowledge graph (GitNexus fork)",
  "scripts": {
    "build:shared": "cd gitnexus-shared && bun run build",
    "build:web": "cd gitnexus-web && bun run build",
    "build:cli": "cd gitnexus && bun run build",
    "build": "bun run build:shared && bun run build:cli && bun run build:web",
    "dev:web": "cd gitnexus-web && bun run dev"
  }
}
